# 更新日志

## 0.6.9

- 移除会锁定编辑器操作的原生底部进度条，仅保留关闭灰色遮罩的独立进度窗口；独立窗口打开失败时也不回退到原生进度条。

## 0.6.8

- 新增独立导入进度窗口，明确显示百分比、当前文件/图层/操作、处理数量和已用时，解决原生底部细进度条未显示状态文字的问题。
- 使用扩展私有消息总线读取最新状态，窗口加载较晚也能获取当前进度；等待确认和任务结束时关闭窗口，关闭或最小化进度窗口不会取消导入。

## 0.6.7

- 新增 EDA 原生导入进度条，显示解压、文件解析、当前图层/文件、图元和钻孔数量，以及复杂区域简化、拆分、填充回退和回滚状态。
- 进度刷新节流，并在复杂运算前让出事件循环以便显示状态；进度显示失败不影响制造图元写入和回滚。
- 增加单任务保护，防止重复点击导入造成并行写入；预览确认、完成、取消和异常退出时清理进度条。

## 0.6.6

- v0.6.5 的再导出结果确认零尺寸 SMD 锡膏不再产生 `NaN` 孔径，但顶层 94 个、底层 14 个普通矩形锡膏开窗仍全部沿 Y 负方向偏移一个自身高度。
- 按 EDA 多边形 API 的 `R x y width height rot round` 左上点语义，普通矩形现在使用 `x = centerX - width / 2`、`y = centerY + height / 2` 写入；旋转矩形的显式轮廓也从左上角开始，避免四点矩形被规范化时把左下点误作左上点。
- README 明确要求在空白 PCB 中验证导入。v0.6.5 测试包的主 PTH 文件含 500 条记录但只有 260 个唯一孔，其中 240 个 via 各重复一次，表明测试文档中叠加保留了上一轮导入的过孔。
- 在空白 PCB 中完成 OpenRX-Gemini 六层板闭环验证：六个铜层、顶/底阻焊、顶/底锡膏、顶/底丝印和板框共 13 层，在 5 µm 光栅比较中均无超过 10 µm 的缺失或多余区域；94 个顶层与 14 个底层普通矩形锡膏全部精确回到原 flash 中心。
- 主 PTH 的 260 个孔、Via 辅助文件的 240 个孔均与 KiCad 原版逐坐标、逐孔径一致，且没有重复孔或 `NaN` 孔径。

## 0.6.5

- 根据 OpenRX-Gemini 六层板在嘉立创 EDA 3.2.175 中再次导出的制造包，定位到 v0.6.4 为关闭原生 SMD 焊盘锡膏而使用“恰好收缩至零尺寸”的参数；EDA 导出器会把这种焊盘写成实际被调用的 `NaN` 锡膏孔径。
- 顶/底铜的 SMD flash 改为无网络实体图形，阻焊与锡膏 flash 也按 Gerber 绝对几何分别写入；只保留通孔焊盘和过孔为原生对象，从源头避免 EDA 自动派生额外阻焊/锡膏以及零尺寸锡膏孔径。
- 矩形 flash 统一写为以 Gerber flash 坐标为中心的闭合多边形，不再使用左上角语义不稳定的 `R` 多边形源；修复 OpenRX-Gemini 锡膏矩形沿 Y 方向偏移一个自身高度的问题。
- 支持嘉立创 EDA 再导出 Gerber 使用的旋转 `Rect` 孔径宏，便于回读和叠图验证。
- 六层样板的主 PTH 文件与原文件 260 个孔逐坐标、逐孔径一致；一键导出的 Via 专用钻孔文件是其中 240 个过孔的重复辅助文件，手动送入其他 CAM 时不得与主 PTH 重复计数。

## 0.6.4

- 修复 ZIP 文件白名单只包含 `.g1/.g2/.g3`、导致六层 OpenRX-Gemini 的 `In4_Cu.g4` 被静默忽略的问题；现在接受 `.g1`～`.g30` 及 `.gp1`～`.gp30`。
- 写入任何图元前，根据 X2 `Copper,Ln` 和最高内层编号计算所需铜层数；当前 PCB 层数不足时只向上扩展并读回验证，失败则停止导入，导入回滚时也尝试恢复原层数。
- 导入成功后将本次涉及的全部铜层设为可见，并把非激活层显示模式恢复为正常亮度，避免图元已经写入但画布看起来只有顶层。
- 完成摘要新增铜层数变化；OpenRX-Gemini 的四个内层应全部进入计划，跳过的自动派生内层铜 flash 数由错误的 780 恢复为 1040。

## 0.6.3

- 读取 KiCad X2 的 `ViaPad`、`SMDPad`、`ComponentPad` 及 Excellon `ViaDrill`、`ComponentDrill` 语义，不再用“钻孔坐标处有阻焊开窗”猜测 via 与通孔焊盘。
- 修复 via-in-pad：跳过由 via 自动派生的铜环时，保留同坐标、同一外层上的独立 SMD 焊盘。OpenRX-Gemini 样本由错误的 225 个 via + 35 个通孔焊盘恢复为源文件中的 240 个 via + 20 个通孔焊盘，并保留顶层 9 个、底层 6 个重叠 SMD 焊盘。
- 对可由原生焊盘精确表示的阻焊开窗，从 Gerber 铜形状与阻焊形状计算焊盘扩展值，不再继承当前 PCB 的默认 2 mil 阻焊扩展；不能精确表示的开窗仍按 Gerber 图形单独写入。
- 禁止原生 SMD 焊盘自动派生锡膏，改为始终写入 Gerber 锡膏 flash，以保留 KiCad 的百分比缩减、偏移以及一焊盘多开窗。

## 0.6.2

- 修复 KiCad `FreePoly` flash 传入复杂多边形焊盘接口时被 EDA 拒绝的问题：改为按绝对坐标写入实体填充，并同步保留其阻焊与锡膏 flash，不依赖原生焊盘自动派生。

## 0.6.1

- 超过 900 个顶点且在 5 µm 内无法安全简化的 Gerber 区域，不再中止整个导入；改用三角剖分及连通分组，无损拆成若干个相邻实体填充，并逐组执行面积一致性校验。
- `OpenRX-Gemini-F_Cu.gtl` 的 5520 点顶层 GND 区域离线拆为 8 个闭合子区域，最大 896 点；整包另有 4 个超限区域完成无损拆分，共生成 29 个子区域。
- 支持 KiCad 10.0.6 的 `RotRect` 旋转矩形孔径宏，以及由单一 outline primitive 构成、以参数指定旋转角度的 `FreePoly` 宏；OpenRX 整包不再出现 D28/D29 或 FreePoly 未定义孔径警告。
- 修复自适应简化倍增步长没有实际测试恰好 5 µm 上限，却在错误信息中声称已测试该上限的问题。

## 0.6.0

- 支持嘉立创 EDA 导出的 5 参数紧凑型 `RoundRect` 宏，不再把其圆角矩形 flash 当作未定义孔径跳过。
- 修复 EDA 焊盘 API 第四个矩形参数在运行时按圆角百分比解释的问题，按 `2 × 半径 ÷ 短边 × 100%` 写入，恢复原始物理圆角。
- 非铜层圆弧改为不超过 10° 的短直线弦段，绕开 EDA 圆弧图元存在但制造 Gerber 不导出的问题。
- 将连续板框线段组装为单一显式闭合 Polyline，替代四条互相独立的 Track，并新增闭合板框统计。
- REF_EVM 板框离线验证为 1 个闭合轮廓、5 个含重复闭合点的顶点；公开 SDK 暂无原生 BoardOutline 创建 API，因此仍需以 EDA 一键导出的板框识别结果为准。
- 原生焊盘继续遵循当前 EDA 阻焊规则；验证环境中再导出的阻焊开窗每边比原 KiCad 样板增加约 0.0508 mm，本版不强制覆盖该规则。

## 0.5.2

- 不再把与 PTH/NPTH 钻孔重合的内层铜 flash 写成独立填充，改由 EDA 的过孔或多层焊盘自动派生内层铜环。
- 保留真正的内层 Gerber 区域（G36/G37），避免误删内层平面铜。
- 完成统计新增“未重复写入的内层铜 flash”；REF_EVM 样本预计跳过 206 个（两个内层各 103 个）。

## 0.5.1

- 修复折线转填充路径经过几何去重后没有重新附加首点，导致 API 返回成功 ID、实际却只生成零面积开放折线的问题。
- 所有点列多边形在写入 EDA 前显式闭合；本样板的 7 个 Gerber 铜区应各自生成一个区域填充。
- 完成统计拆分为“区域填充”和“Flash 填充”，避免圆形/矩形 flash 填充掩盖铜区创建失败。

## 0.5.0

- 参考 `REF_EVM.kicad_pcb` 离线核对 Gerber：X 坐标保持、Y 坐标取反；KiCad RoundRect 四角已经编码有效焊盘旋转，扩展不再额外反转角度（例如 225° 与 Gerber -135°等价）。
- 普通填充失败时，优先创建闭合折线并调用官方 `convertToFill()`，成功后删除临时折线，恢复“一块铜区一个填充图元”。
- 顶/底铜 flash 创建原生焊盘后，跳过同坐标的阻焊/锡膏 flash，避免重复生成焊盘派生层。
- 联合铜层、阻焊和 Excellon 判断钻孔类型：有阻焊开窗的钻孔重建为通孔焊盘，无开窗的钻孔重建为盖油过孔。
- 盖油过孔显式设置顶/底负阻焊扩展，不再跟随可能开窗的 EDA 默认规则。
- Gerber 与 Excellon 坐标使用 5 µm 距离容差匹配，兼容本样本最大约 1.61 µm 的英制钻孔量化误差。
- 完整样本推断结果与源 PCB 一致：132 个 SMD 焊盘、24 个通孔焊盘、79 个双面盖油过孔，并跳过 312 个由焊盘自动派生的 mask/paste flash。

## 0.4.0

- 移除 `PCB_PrimitiveImage` 填充回退；该 API 在不同轮廓上会出现 180°旋转或 Y 镜像，无法可靠保持 Gerber 方向。
- 铜层、阻焊、丝印、焊膏和装配等填充失败时，统一改用同层原生线条按 Gerber 绝对坐标扫描成实体。
- 扫描段直接使用原始坐标，不存在图像锚点、缩放、旋转或镜像变换。
- 扫描线按每批 100 条写入，避免复杂铜区一次并发数千个 API 调用。
- 新增非对称多边形扫描方向回归测试。

## 0.3.3

- 修复矢量图像回退错误使用包围盒 `minY` 作为左上角，导致铜区和阻焊向下偏移一个自身高度的问题。
- 图像局部坐标改为从 `maxY` 向下计算，符合 EDA 图像 BBox 左上角坐标语义。
- 修复 X2 属性中的器件名（例如 `%TO.C,Y1*%`、`X1`）被误判为坐标，导致 Y≈0 位置凭空出现焊盘或阻焊的问题。
- 新增图像定位和含坐标字母 X2 属性的回归测试。

## 0.3.2

- 修复模态 `D03` 在 `%TO/%TD` 等无坐标 X2 属性命令上被错误重复执行的问题，消除同一坐标出现 0°/45°等不同孔径叠加。
- 非铜层填充改为真正的两参数调用，不再传递网络参数。
- 顶/底层焊膏新增分级回退：尝试矢量图像后，以焊膏层细密线条扫描形成实体，避免运行时填充 API 不可用时中止整个导入。
- 完成提示新增焊膏扫描图元与线条数量统计。
- 批量回滚失败时逐个重试删除本次创建的图元，降低失败导入留下重叠图形的概率。

## 0.3.1

- 修复顶层/底层阻焊闪光可能因填充 API 可选参数不兼容而导入失败的问题。
- 填充 API 改用显式空网络的最小参数形式。
- 将矢量图像回退扩展到铜层、阻焊、丝印、装配等受支持图层，并同时覆盖 flash 与 region。
- 圆形和矩形闪光使用精确矢量图像，圆角矩形、长圆形和正多边形使用闭合多边形回退。

## 0.3.0

- 参考嘉立创 EDA Pro 自身导出的 Gerber，兼容未先选择孔径就开始 `G36/G37` 铜区的写法。
- 普通填充 API 拒绝有效铜区时，自动用 `PCB_PrimitiveImage` 在同一铜层创建等价多边形实体。
- 新增图像区域回退统计并纳入失败回滚。

## 0.2.2

- 对超过 900 个顶点的 Gerber 区域执行自适应 RDP 安全简化，最大允许几何偏差为 5 µm。
- `REF_EVM-CuBottom` 的 1187 顶点区域在 0.001 µm 容差下缩减到 890 个顶点，面积误差约为百万分之 0.02。
- 完成提示会报告被简化的区域数量和实际最大容差。

## 0.2.1

- 修正 `G36/G37` 区域错误继承前一个图元坐标、导致 KiCad 覆铜轮廓自交和填充创建失败的问题。
- 修正 `G36/G37` 被误识别为 `G03` 圆弧插补的前缀匹配问题。
- 导入失败信息现在包含文件名、EDA 图层、图元类型和图元序号。

## 0.2.0

- 修正 KiCad X2 `Copper,L1,Top / L4,Bot / Inr` 顶层、底层和内层映射。
- 支持 KiCad `RoundRect` 圆角矩形孔径宏，不再产生未定义 D 码警告。
- 根据 Excellon FileFunction 区分 PTH 与 NPTH，并将 NPTH 写为非金属化孔。
- 使用 `GERBER-REF_EVM.zip` 做整包解析回归。

## 0.1.0

- 首个可安装 MVP。
- 支持常用 RS-274X Gerber 图元和 Excellon 圆孔。
- 支持 ZIP、多文件、图层自动映射、导入预览和失败回滚。
